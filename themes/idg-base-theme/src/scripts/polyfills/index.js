/**
 * This should not be needed as Babel 7 should polyfill based on
 * what is required to meet what is configured in `@babel/preset-env` / `.browserslistrc`.
 *
 * Should a manual polyfill be required, `core-js` is available (github.com/zloirock/core-js).
 * e.g import 'core-js/fn/array/from';
 *
 * Then in App.js, import './polyfills';
 */
